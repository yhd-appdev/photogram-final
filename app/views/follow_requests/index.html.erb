<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      List of all follow requests
    </h1>
  </div>
</div>

<hr>

<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h2>
      Add a new follow request
    </h2>

    <form action="/insert_follow_request" method="post">
      <div class="custom-control custom-checkbox mb-3">
        <input type="checkbox" class="custom-control-input" id="status_box" name="query_status" value="1">

        <label class="custom-control-label" for="status_box">Status</label>
      </div>

      <div class="form-group">
        <label for="recipient_id_box">
          Recipient
        </label>

        <input type="text" id="recipient_id_box" name="query_recipient_id" class="form-control">
      </div>

      <div class="form-group">
        <label for="sender_id_box">
          Sender
        </label>

        <input type="text" id="sender_id_box" name="query_sender_id" class="form-control">
      </div>

      <button class="btn btn-block btn-outline-secondary">
        Create follow request
      </button>
    </form>
  </div>
</div>

<hr>
<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Status
        </th>

        <th>
          Recipient
        </th>

        <th>
          Sender
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @follow_requests.each do |follow_request| %>
      <tr>
        <td>
          <%= follow_request.id %>
        </td>

        <td>
          <%= follow_request.status %>
        </td>

        <td>
          <%= follow_request.recipient_id %>
        </td>

        <td>
          <%= follow_request.sender_id %>
        </td>

        <td>
          <%= time_ago_in_words(follow_request.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(follow_request.updated_at) %> ago
        </td>

        <td>
          <a href="/follow_requests/<%= follow_request.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<hr>
